[pytest]
# Permite que las pruebas en pruebas/ hagan "import redactor", "import
# validador", etc. sin instalar el proyecto como paquete: se agrega esta
# carpeta (diagnostico-ia/) al sys.path.
pythonpath = .
testpaths = pruebas
